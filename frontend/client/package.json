{
  "name": "room-frontend-clientside",
  "version": "1.0.0",
  "description": "home page client code",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --sourcemap --minify --outdir=dist",
    "watch": "esbuild src/index.ts --bundle --sourcemap --outdir=dist --watch",
    "analyze": "esbuild src/index.ts --bundle --sourcemap --minify --outdir=dist --analyze=verbose",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pauline-sh@proton.me",
  "license": "ISC",
  "dependencies": {
    "lit": "^2.7.5"
  },
  "devDependencies": {
    "esbuild": "^0.18.10",
    "eslint": "^8.43.0",
    "prettier": "^2.8.8",
    "typescript": "^5.1.6"
  }
}
